# 🚀 Quick Start - Nuxni Pitnik

## ⚡ เริ่มต้นใช้งานภายใน 5 นาที

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

เปิด Terminal/PowerShell ในโฟลเดอร์ `nuxni-pitnik` และรันคำสั่ง:

```bash
npm install
```

หรือใช้ package manager อื่น:

```bash
# pnpm (แนะนำ - เร็วกว่า)
pnpm install

# yarn
yarn install
```

**⏱️ ใช้เวลาประมาณ 2-3 นาที**

---

### ขั้นตอนที่ 2: รัน Development Server

```bash
npm run dev
```

คุณจะเห็นข้อความคล้ายๆ นี้:

```
✔ Nuxt DevTools is enabled! Press Shift + Alt + D to open.
  
  ➜ Local:   http://localhost:3000/
  ➜ Network: use --host to expose
```

---

### ขั้นตอนที่ 3: เปิดเบราว์เซอร์

เปิดเบราว์เซอร์และไปที่:

```
http://localhost:3000
```

**🎉 คุณจะเห็นหน้า Welcome/Onboarding!**

---

## 📱 ทดสอบหน้าต่างๆ

### เส้นทางหลัก (Main Routes)

| หน้า | URL | คำอธิบาย |
|------|-----|----------|
| 🏠 Welcome | `/` | หน้าต้นรับ Onboarding |
| 🔐 Login | `/login` | หน้าเข้าสู่ระบบ |
| 📰 Newsfeed | `/newsfeed` | ฟีดหลัก (หน้าแรก) |
| 👤 Profile | `/timeline` | โปรไฟล์ผู้ใช้ |
| 👥 Groups | `/groups` | กลุ่มต่างๆ |
| 📅 Events | `/events` | อีเวนต์ |
| 🛒 Marketplace | `/marketplace` | ตลาดสินค้า |
| 💬 Messages | `/messages` | ข้อความ |
| 🔔 Notifications | `/notifications` | การแจ้งเตือน |
| ⚙️ Settings | `/settings` | ตั้งค่า |

### ทดสอบ Flow ปกติ

1. เริ่มที่ `/` (Welcome)
2. คลิก "Getting Started"
3. ไปที่ `/login` (Login)
4. คลิก "Sign In"
5. ไปที่ `/newsfeed` (Main Feed)
6. เล่นกับฟีเจอร์ต่างๆ

---

## 🎨 ปรับแต่ง UI

### เปลี่ยนสี Primary

แก้ไขไฟล์ `tailwind.config.ts`:

```typescript
colors: {
  primary: {
    // เปลี่ยนเป็นสีน้ำเงิน
    500: '#3b82f6',
    600: '#2563eb',
    700: '#1d4ed8',
    // ... หรือใช้ colors อื่น
  }
}
```

### เปลี่ยน Logo

1. เพิ่มรูปโลโก้ใน `public/images/logo.png`
2. แก้ไข path ใน `components/App/Header.vue`

### เปลี่ยน Mock Data

แก้ไขไฟล์ใน `composables/`:
- `usePosts.ts` - ข้อมูลโพสต์
- `useUserProfile.ts` - ข้อมูลผู้ใช้

---

## 🐛 Troubleshooting

### ปัญหา: Port 3000 ถูกใช้งานอยู่

```bash
# ใช้ port อื่น
PORT=3001 npm run dev
```

### ปัญหา: Module not found

```bash
# ลบและติดตั้งใหม่
rm -rf node_modules
rm package-lock.json
npm install
```

### ปัญหา: Tailwind ไม่ทำงาน

1. ตรวจสอบ `nuxt.config.ts` มี `@nuxtjs/tailwindcss` ใน modules
2. ตรวจสอบไฟล์ `assets/css/main.css` มี `@tailwind` directives
3. Restart dev server (Ctrl+C แล้ว `npm run dev` ใหม่)

### ปัญหา: Icons ไม่แสดง

```bash
# ติดตั้ง @nuxt/icon ใหม่
npm install @nuxt/icon
```

---

## 📝 คำสั่งที่ใช้บ่อย

```bash
# Development
npm run dev              # รัน dev server

# Build & Preview
npm run build            # Build for production
npm run preview          # Preview production build

# Generate Static
npm run generate         # สร้าง static site

# Clean
rm -rf .nuxt .output     # ลบ cache (Windows: rmdir)
```

---

## 🎯 Next Steps

### เรียนรู้เพิ่มเติม
1. อ่าน `README.md` - เอกสารหลักฉบับเต็ม
2. อ่าน `SETUP.md` - คู่มือ Setup แบบละเอียด
3. อ่าน `MIGRATION_SUMMARY.md` - สรุปการแปลงโปรเจค

### เริ่มพัฒนา
1. สร้าง Component ใหม่ใน `components/`
2. เพิ่มหน้าใหม่ใน `pages/`
3. สร้าง Composable ใน `composables/`
4. ปรับแต่ง Layout ใน `layouts/`

### Deploy
1. Build: `npm run build`
2. Deploy to Vercel/Netlify/Your hosting
3. ดูคำแนะนำใน `SETUP.md`

---

## 💡 Tips

- 🔥 **Hot Reload**: บันทึกไฟล์แล้วเห็นผลทันที ไม่ต้อง refresh
- 🎨 **Tailwind IntelliSense**: ติดตั้ง extension ใน VS Code
- 🐛 **Vue DevTools**: ติดตั้งเพื่อ debug ง่ายขึ้น
- 📱 **Responsive Testing**: ใช้ DevTools responsive mode (F12)
- ⚡ **Auto Import**: Components และ Composables import อัตโนมัติ

---

## 🆘 ต้องการความช่วยเหลือ?

- 📖 อ่าน Documentation: `README.md`
- 🔍 ค้นหาใน [Nuxt Docs](https://nuxt.com)
- 💬 เปิด Issue ใน GitHub
- 📧 ติดต่อทีมพัฒนา

---

**✨ Happy Coding!**

Made with ❤️ using Nuxt.js, Vue 3, and Tailwind CSS
