# วิธีติดตั้งและรันโปรเจค

## ⚡ Quick Start

### 1. เปิด Terminal ใน VS Code
กด `Ctrl + ~` หรือ `View > Terminal`

### 2. เข้าไปยังโฟลเดอร์โปรเจค
```powershell
cd nuxni-pitnik
```

### 3. ติดตั้ง Dependencies
```powershell
npm install
```
รอประมาณ 2-3 นาที

### 4. รัน Development Server
```powershell
npm run dev
```

### 5. เปิดเบราว์เซอร์
```
http://localhost:3000
```

## ✅ เสร็จแล้ว!

หลังจากรันคำสั่งเหล่านี้:
- ✅ TypeScript errors จะหายไป
- ✅ Auto-imports จะทำงาน
- ✅ โปรเจคพร้อมใช้งาน

## 🔄 คำสั่งที่ใช้บ่อย

```powershell
# Development
npm run dev              # รัน dev server

# Build
npm run build            # Build production

# Preview
npm run preview          # Preview production

# Clean
Remove-Item -Recurse -Force .nuxt, .output
```

## 📝 หมายเหตุ

Errors ที่เห็นตอนนี้เป็นเรื่องปกติ จะหายหลังจาก `npm install` และ `npm run dev`
