<?xml version="1.0" encoding="UTF-8"?>
<!--
    โปรไฟล์รันเทสต์บน **MySQL จริง** (ปกติ `phpunit.xml` ใช้ sqlite :memory:)

    ใช้เมื่อ: ต้องพิสูจน์พฤติกรรมที่ sqlite โกหกให้ไม่ได้ — unique/index จริง, STRICT mode,
    ชนิดคอลัมน์ TIME/DATETIME, ความยาวชื่อ index 64 ตัวอักษร, การเทียบสตริงตาม collation

    วิธีรัน (ครั้งแรกต้องสร้างสคีมาก่อน):
        php artisan test:db:rebuild
        php artisan test -c phpunit.mysql.xml

    หมายเหตุ: ไม่มี migrate:fresh ในโหมดนี้ (migrate จากศูนย์บน MySQL ยังพังอยู่ — G25)
    สคีมามาจากการคัดลอกโครงของ DB dev · ทุกเทสต์ห่อด้วย transaction แล้ว rollback
    รหัสผ่าน/ผู้ใช้ฐานข้อมูลยังอ่านจาก .env ตามเดิม — ไฟล์นี้ไม่มีความลับ
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- เหตุผลของ 2G อยู่ใน phpunit.xml (xdebug เปิดแล้วทะลุ 512M) -->
        <ini name="memory_limit" value="2G"/>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="nuxnan_testing"/>
        <env name="TEST_DB_PREBUILT" value="1"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="JWT_SECRET" value="testing_secret_key_at_least_32_characters_long"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
